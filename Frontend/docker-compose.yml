services:
  frontend:
    build:
      context: .
    container_name: siem-frontend
    ports:
      - "5173:5173"
    volumes:
      - ./src:/app/src
      - /app/node_modules
    restart: unless-stopped
