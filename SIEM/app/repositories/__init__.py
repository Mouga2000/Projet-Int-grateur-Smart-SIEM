# app/repositories/__init__.py
# Package pour les repositories (accès aux données Elasticsearch)
#
# Chaque repository encapsule les opérations CRUD sur un index ES.
# Pattern : UserRepository(es) -> opérations sur l'index "users"
