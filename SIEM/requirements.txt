# ============================================================
# Smart SIEM - Dépendances Python (requirements.txt)
# ============================================================

# --- Framework Web ---
fastapi==0.138.0
uvicorn[standard]==0.49.0
python-multipart==0.0.32

# --- Authentification ---
python-jose[cryptography]==3.5.0   # JWT
passlib[bcrypt]==1.7.4              # Hachage de mots de passe
bcrypt==5.0.0
pyotp==2.10.0                       # MFA TOTP
qrcode[pil]==8.2                    # QR codes pour la MFA
pillow==12.2.0                      # Nécessaire pour qrcode[pil]

# --- PostgreSQL (données structurées : utilisateurs, règles, audits…) ---
sqlalchemy==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.1

# --- Elasticsearch (logs, alertes — données volumineuses) ---
elasticsearch==8.19.3

# --- Validation ---
pydantic==2.13.4
pydantic-settings==2.0.3
email-validator==2.3.0              # Validation des emails (EmailStr)

# --- Utilitaires ---
python-dotenv==1.2.2
